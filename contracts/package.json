{
  "name": "medivet-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for MediVet platform on 0G Network",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run deploy.js --network galileo",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0",
    "dotenv": "^16.0.0"
  }
}
